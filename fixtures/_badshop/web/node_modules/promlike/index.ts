import { Counter } from 'prom-client';

// Installed dependency: not ours to fix, never reported.
export const vendored = new Counter({
  name: 'promlike_requests_total',
  help: 'Requests.',
  labelNames: [
    'user_id', // notwant: metrics/high-cardinality-label
  ],
});
